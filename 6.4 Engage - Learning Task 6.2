<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SVG Animated Flower Power</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #ffeef2; /* Soft pink background */
            font-family: 'Comic Sans MS', cursive, sans-serif; /* Cute font */
            margin: 0;
            flex-direction: column;
            color: #ff69b4; /* Hot pink text */
        }

        h1 {
            margin-bottom: 30px;
            font-size: 2.5em;
            text-shadow: 2px 2px 4px rgba(255,105,180,0.3);
        }

        .flower-svg {
            border: none; /* No border for a softer look */
            background-color: #fff;
            box-shadow: 0 8px 16px rgba(255,105,180,0.2); /* Pinker shadow */
            border-radius: 50%; /* Make it round */
            padding: 20px;
            animation: bounce 2s infinite ease-in-out; /* Add a subtle bounce to the whole SVG */
        }

        /* Flower Center */
        .flower-center {
            fill: #ffd700; /* Gold */
            stroke: #ff9900; /* Orange-gold border */
            stroke-width: 3;
            animation: pulse-center 2s infinite alternate; /* Center pulses */
        }

        /* Petals */
        .petal {
            fill: #ffb6c1; /* Light pink */
            stroke: #ff69b4; /* Hot pink border */
            stroke-width: 2;
            transform-origin: 50% 50%; /* Rotate around the center of the SVG */
            animation: petal-sway 3s infinite ease-in-out;
        }

        /* Individual petal animation delays for a staggered effect */
        .petal:nth-child(1) { animation-delay: 0s; }
        .petal:nth-child(2) { animation-delay: 0.3s; }
        .petal:nth-child(3) { animation-delay: 0.6s; }
        .petal:nth-child(4) { animation-delay: 0.9s; }
        .petal:nth-child(5) { animation-delay: 1.2s; }
        .petal:nth-child(6) { animation-delay: 1.5s; }

        /* Keyframe Animations */

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        @keyframes pulse-center {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        @keyframes petal-sway {
            0%, 100% { transform: rotate(0deg) scale(1); opacity: 1; }
            50% { transform: rotate(10deg) scale(0.98); opacity: 0.9; }
        }

    </style>
</head>
<body>

    <h1>Flower Power Loading!</h1>

    <svg class="flower-svg" width="300" height="300" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
        
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(0 50 50)" />
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(60 50 50)" />
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(120 50 50)" />
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(180 50 50)" />
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(240 50 50)" />
        <path class="petal" d="M50,50 Q40,20 50,5 C60,20 50,50 50,50 Z" transform="rotate(300 50 50)" />

        <circle cx="50" cy="50" r="15" class="flower-center" />

    </svg>

</body>
</html>
